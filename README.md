# Simple CRUD with Node, React and MongoDB

As the name says, its a basic application, just for reference, don't need env vars or anything, just install the dependencies for frontend and backend and run.
